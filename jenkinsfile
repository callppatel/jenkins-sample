pipeline {
    agent any
    stages {
        stage('Print Parameter from API') {
            steps {
                echo "Hello"
                echo "MY_VAR: ${params.MY_VAR}"
            }
        }
    }
}
